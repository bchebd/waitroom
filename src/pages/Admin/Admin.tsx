import type { FC } from "react";

import { AdminTable } from "@/sections/AdminTable/AdminTable";

const Admin: FC = () => {
    return (
        <main>
            <AdminTable />
        </main>
    );
};

export default Admin;